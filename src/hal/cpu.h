#pragma once

/// Hardware Abstraction Layer for the CPU

namespace hal {
namespace cpu {

    /// CPU init routines (not really necessary for the AVR)
    void Init();

} // namespace cpu
} // namespace hal
